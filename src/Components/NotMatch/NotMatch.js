import React from 'react';
import './NotMatch.css'

const NotMatch = () => {
    return (
        // no match image
        <div className="image">
            
            
        </div>
    );
};

export default NotMatch;
